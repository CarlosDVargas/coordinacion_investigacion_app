<%= form_with(model: project) do |form| %>
  <% if project.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
        <% project.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Código", style: "display: block" %>
    <%= form.number_field :code %>
  </div>

  <div>
    <%= form.label "Nombre", style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <label for="main_investigator_email" style="display: block">Correos investigador principal</label>
  <input id="main_investigator_email" name="main_investigator_email" type="text_field" />

  <div data-controller="add-investigator" data-add-investigator-url="<%= register_project_investigators_path(@investigators) %>">
    <label for="associated_investigator_email" style="display: block">Correos investigadores asociados</label>
    <input id="associated_investigator_email" name="associated_investigator_email" type="text_field" />
    <a class="btn btn-sm btn-info" name="addInvestigator" href="#" data-action="add-investigator#change">Agregar investigador</a>
  
    <br>
    <div id="investigators">
      <div class="row">
        <div class="col-9" data-add-investigator-target="elements">  
          <%= render partial: 'associated_investigator_list', locals: { investigators: @investigators} %>
        </div>
      </div>
    </div>
  </div>
  <div>
    <%= form.submit(project.new_record? ? "Registrar" : "Actualizar", class: "btn btn-sm btn-success") %>
  </div>
<% end %>