<table id="associated_investigator_table" class="table table-bordered table-hover">
          <thead>
            <tr>
              <th scope="col">Nombre Completo</th>
              <th scope="col">Correo</th>
              <th scope="col">Acción</th>
            </tr>
          </thead>
          <tbody>
            <% investigators.each do |investigator| %>
              <tr>
                <td><%= investigator.name + " " + investigator.lastname %></td>
                <td><%= investigator.email %></td>
                <td><a class="btn btn-sm btn-danger" name="removeInvestigator" href="#" data-action="add-investigator#delete">Eliminar investigador</a></td>
              </tr>
            <% end %>
          </tbody>
</table>